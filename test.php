if(empty(stim($fname))){
    $error.="Bạn chưa nhập array!"
} 
if(empty(stim($lname))){
    $error.="Bạn chưa nhập array!"
} 
if(empty(stim($date ))){
    $error.="Bạn chưa nhập array!"
}
if(empty(stim($gender))){
    $error.="Bạn chưa nhập array!"
}  
if(empty(stim($phone))){
    $error.="Bạn chưa nhập array!"
} 